/*
** EPITECH PROJECT, 2019
** store_board.c
** File description:
** linked list to store board input
*/

#include "node.h"

node_t *store_board(char *str)
{
    // Here we'll send the head to the rest of the program
    // we'll have to store the head somewhere still!
    if (check_string(str) != 1)
        invalid_string();
}


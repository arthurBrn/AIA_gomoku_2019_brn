/*
** EPITECH PROJECT, 2019
** empty_list.c
** File description:
** Check list empty or not
*/

#include "global.h"

int empty_list(node_t *head)
{
    if (head == NULL)
        return (1);
    return (0);
}
